# Backend

This folder is reserved for the backend API implementation.

- `server.js` contains a starter Express server.
- Add routes, controllers, models, and authentication here.
- Run `npm install` inside `backend` before starting the server.
